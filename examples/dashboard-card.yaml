# Family Task Card — minimal dashboard usage
#
# The Family Task Card integration serves and registers the card automatically
# (see README) — no manual dashboard resource needed. Add it to a dashboard:
# Edit dashboard → Add card → "Family Task Card".
#
# Configuration options (family members, task lists / providers, points and
# rewards) are on the roadmap; see ROADMAP.md. For now:

type: custom:family-task-card
title: Familien-Aufgaben
