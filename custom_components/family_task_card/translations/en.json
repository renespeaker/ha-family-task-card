{
  "config": {
    "step": {
      "user": {
        "title": "Family Task Card",
        "description": "Choose the feeds Home Assistant should load server-side. They appear as sensor.family_task_<key> and are picked up automatically by the Family Task Card.",
        "data": {
          "presets": "Standard feeds",
          "custom": "Custom feeds (one per line: Name | https://…/feed.xml)",
          "scan_interval": "Update interval (minutes)"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Family Task Card is already configured. Use its options to change the feeds."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Family Task Card feeds",
        "description": "Change which feeds are loaded and how often.",
        "data": {
          "presets": "Standard feeds",
          "custom": "Custom feeds (one per line: Name | https://…/feed.xml)",
          "scan_interval": "Update interval (minutes)"
        }
      }
    }
  }
}
