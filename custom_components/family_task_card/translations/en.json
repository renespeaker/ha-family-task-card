{
  "config": {
    "step": {
      "user": {
        "title": "Family Task Card",
        "description": "Add the Family Task Card. This installs and registers the Lovelace card. Family members, task lists and rewards are configured later (see ROADMAP)."
      }
    },
    "abort": {
      "single_instance_allowed": "Family Task Card is already configured."
    }
  }
}
